# Scraped output — can be regenerated by running main.py; individual zips run 400MB-1GB+,
# well over GitHub's 100MB per-file hard limit. Keep the small run-summary log, not the data.
Results/*
!Results/_run_summary.json

# Python
__pycache__/
*.pyc
relief_web/

# macOS
.DS_Store
